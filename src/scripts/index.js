import 'queryselector.js';
import 'addlistener.js';

import ES6Promise from 'es6-promise';
import Raven from 'raven-js';

// if (!Config.debug && window.blokks) {
// 	const userContext = {
// 	   name: window.blokks.account.name,
// 	   email: window.blokks.account.email,
// 	};

// 	Raven.config(Config.sentry).install();
// 	Raven.setUserContext(userContext);
// }

// ES6Promise.polyfill();
// Router.initialize();
